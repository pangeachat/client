// The tutorial card renders `BotFace`, whose native loads fail asynchronously
// in the test VM exactly as they do for the onboarding steps — same filter.
export '../onboarding_tests/flutter_test_config.dart';
